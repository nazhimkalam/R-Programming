# installing packages using script, because when sharing codes with people,
# there are also able to see what packages are installed and install them as
# well.
install.packages('LiblineaR')

require("LiblineaR")
 